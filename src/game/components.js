export const Position = (x, y) => ({ x, y });
export const Velocity = (x, y) => ({ x, y });
export const Size = (w, h) => ({ w, h });
export const Color = (hex) => hex;

export const Player = () => ({ tag: true });
export const Enemy = () => ({ tag: true });
